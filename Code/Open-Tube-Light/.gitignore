# ── CMake build output ────────────────────────────────────────────────
build/

# ── Legacy STM32CubeIDE Makefile build (safe to delete entirely) ─────
Debug/
Release/

# ── Eclipse / STM32CubeIDE metadata (no longer primary with CMake) ───
.settings/
.metadata/
.cproject
.project
.mxproject

# ── Reference PDFs (large binaries, not source) ─────────────────────
*.pdf

# ── OS / editor junk ────────────────────────────────────────────────
Thumbs.db
.DS_Store
